# ENV Variable Archaeologist

Digs through your codebase to find all environment variables, showing where they're used and which ones are actually set. Run: `python env_archaeologist.py <directory>` to start the excavation.